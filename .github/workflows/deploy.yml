name: E-Hibah CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Test Pipeline
        run: echo "E-Hibah CI/CD Running Successfully"
